%h4= I18n.t("menu.import")

- if @error_messages
  #error_explanation
    %h2=# "#{pluralize(@error_messages.count, "error")} prohibited this record from being saved:"
    %ul
      - @error_messages.each do |msg|
        %li= msg



= form_tag csv_import_path, multipart: true do |f|
  = label_tag :csv_file
  = file_field_tag :csv_file
  = submit_tag
