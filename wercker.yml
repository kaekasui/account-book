box: wercker/rvm
services:
  - wercker/mysql
build:
  steps:
    - bundle-install
    - rails-database-yml
    - script:
        name: Set up DB
        code: RAILS_ENV=test bundle exec rake db:schema:load
  after-steps:
    - hipchat-notify:
      token: XZhgAPWJm6aCvXWaWNXVjsbU3Krqkwhsq0AgUbhQ
      room-id: account-book-pig
      from-name: ci
